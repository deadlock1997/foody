import { SxProps, Theme } from "@mui/material";

export const footerContainer: SxProps<Theme> = {
  py: 2,
  textAlign: "center",
  width: "100%",
};

export const footerDivider: SxProps<Theme> = {
  my: 2,
  borderColor: "text.primary",
};